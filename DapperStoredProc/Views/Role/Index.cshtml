@model IEnumerable<DapperStoredProc.DTO.UserRolePartial>

@{
    ViewData["Title"] = "Index";
    //Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="CreateRole">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                  
            <th>
                RoleId
            </th>
            <th>
                RoleName
            </th>
            <th>Users</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>            
            <td>
                @item.RId
            </td>
            <td>
                @item.RName
            </td>
            <td>
               
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RId">Edit</a> |
                <a class="confimDeletion" asp-action="Delete" asp-route-id="@item.RId">Delete</a>

            </td>
        </tr>
}
    </tbody>
</table>
